Django==5.2.13
django-tenants==3.10.1
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
psycopg[binary]==3.2.*
django-redis==5.4.*
PyJWT==2.9.0
gunicorn==23.*
celery[redis]>=5.4,<6    # major cap: the SchedEntry(dict)+scope-attr contract relies on how Celery
                         # consumes a beat entry via **entry (commons.platform.beat) — vet a 6.x bump
croniter>=2.0            # per-tenant tz cron evaluation for the fanout scheduler (tenants.tasks)
celery-redbeat>=2.2,<3   # HA beat (Redis-lock leader election); activated MT-only via CELERY_BEAT_SCHEDULER.
                         # major cap for the same SchedEntry/**entry reason as celery above
boto3>=1.34            # S3 uploads for offline GPS coordinates (routes.services)
# uvicorn[standard]==0.34.*   # only needed for the async path (UvicornWorker/ASGI),
                              # which this project intentionally does not use —
                              # see the "Worker model" note in tenants_back/settings.py
